package GitAndGitHub;

public class AllCommands {

	
	
	// All the command for Daily use in Company
	
	/*
	 * 1) git init   :- create new local repository
	 * 
	 * 2) git status :- to see your local repository/branch status
	 * 
	 * 3) git remote add origin => (To connect with Central Repository) => (paste your Central Repository link from GitHub)
	 * 
	 * 4) git add .  :- to add all the files (in buffer so as commit and push)
	 * 
	 * 5) git add filePath :- to add specific file
	 * 
	 * 6)  git commit -m "massage" :- before push  must needed action with proper massage
	 *
	 * 7) git push --set-upstream "branch name" :- when you want to push in specific branch and also set remote for that branch
	 * 
	 * 8) git push :- if you have set remote already then and then you can directly push  (git remote add origin) or above step
	 *    for 1st time push you need to set remote from 2nd time you can directly push.
	 *  
	 * 9) git pull :- dirctly pull the changes in you local repo from central repo when anyone post in on central repo 
	 * 
	 * 10) git pull origin master/main :- Another way to pull if normal pull not works 
	 * whatever origin in central repo main/master from where you trying to pull code mention there.
	 * 
	 * 
	 *  git branch --set-upstream-to=origin Rushi     this is also on way to set upstream origin to any branch 
	 *  
	 */
	
	public static void main(String []args) {
		
		System.out.println("Sai Plaza");
		System.out.println("Sangamner");
		System.out.println("Ghulewadi");
		System.out.println("AmrutNagar");
		System.out.println("AVCOE");
		System.out.println("Rushikesh Awari");
	}
	
}
